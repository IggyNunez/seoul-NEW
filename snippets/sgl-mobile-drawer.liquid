{%- comment -%}
  Seoul Glow Lab - Mobile Navigation Drawer (Fully Customizable)
  All text, URLs, video, quick links, and product limits controlled via SGL Header schema.
{%- endcomment -%}

{%- comment -%} ── Overlay ── {%- endcomment -%}
<div class="sgl-drawer-overlay" id="sgl-drawer-overlay" aria-hidden="true"></div>

{%- comment -%} ── Drawer Panel ── {%- endcomment -%}
<div class="sgl-drawer{% if drawer_glassmorphic %} sgl-drawer--glass{% endif %}" id="sgl-drawer" role="dialog" aria-modal="true" aria-label="Navigation menu" style="--drawer-logo-height: {{ drawer_logo_height | default: 28 }}px;">

  {%- comment -%} Header {%- endcomment -%}
  <div class="sgl-drawer__header">
    {%- comment -%} Use drawer_logo_image if set, else fall back to header logo_image {%- endcomment -%}
    {%- assign effective_logo = drawer_logo_image | default: logo_image -%}
    <div class="sgl-drawer__logo">
      {%- if effective_logo != blank -%}
        {{ effective_logo | image_url: width: 160 | image_tag: class: 'sgl-drawer__logo-img', alt: logo_text | default: shop.name, loading: 'lazy' }}
      {%- else -%}
        <span class="sgl-drawer__logo-text">{{ logo_text | default: 'SEOUL GLOW LAB' }}</span>
      {%- endif -%}
    </div>
    <div class="sgl-drawer__header-actions">
      <button class="sgl-drawer__header-btn" aria-label="Search" onclick="document.querySelector('.sgl-drawer').classList.remove('active'); document.querySelector('.sgl-drawer-overlay').classList.remove('active'); document.body.style.overflow = ''; document.querySelector('#search-modal')?.showDialog?.();">
        {%- render 'sgl-icon', icon: 'search', size: 20 -%}
      </button>
      <button class="sgl-drawer__close" data-sgl-drawer-close aria-label="Close menu">
        {%- render 'sgl-icon', icon: 'close', size: 20 -%}
      </button>
    </div>
  </div>

  {%- comment -%} Scrollable Content {%- endcomment -%}
  <div class="sgl-drawer__content">

    {%- comment -%} Featured Promo Card (CRO: entire card clickable when promo_link set) {%- endcomment -%}
    {%- if promo_link != blank -%}
      <a href="{{ promo_link }}" class="sgl-drawer__featured sgl-drawer__featured--clickable">
    {%- else -%}
      <div class="sgl-drawer__featured">
    {%- endif -%}
      <div class="sgl-drawer__featured-content">
        <div class="sgl-drawer__featured-visual">
          {%- if promo_image -%}
            {{ promo_image | image_url: width: 200 | image_tag: loading: 'lazy', class: 'sgl-drawer__featured-img' }}
          {%- elsif video_url != blank -%}
            <video class="sgl-drawer__featured-video" autoplay loop muted playsinline>
              <source src="{{ video_url }}" type="video/mp4">
            </video>
          {%- else -%}
            <div class="sgl-drawer__featured-placeholder">
              {%- render 'sgl-icon', icon: 'sparkles', size: 24 -%}
            </div>
          {%- endif -%}
        </div>
        <div class="sgl-drawer__featured-text">
          <span class="sgl-drawer__featured-label">
            {%- render 'sgl-icon', icon: 'sparkles', size: 12 -%}
            {{ promo_title | default: 'New Collection' }}
          </span>
          <p class="sgl-drawer__featured-desc">{{ promo_subtitle | default: 'Infinity Mirror Series now available' }}</p>
          <span class="sgl-drawer__featured-link" aria-hidden="true">
            {{ mobile_promo_btn_text | default: 'Shop Now' }} {%- render 'sgl-icon', icon: 'arrow-right', size: 14 -%}
          </span>
        </div>
      </div>
    {%- if promo_link != blank -%}
      </a>
    {%- else -%}
      </div>
    {%- endif -%}

    {%- comment -%} Navigation Items {%- endcomment -%}
    <nav class="sgl-drawer__nav" aria-label="Main navigation">
      {%- for link in linklists[menu].links -%}
        {%- assign link_collection = collections[link.handle] -%}
        {%- assign link_icon = link.handle -%}
        {%- assign link_badge = '' -%}
        {%- if link_collection.metafields.sgl.icon != blank -%}
          {%- assign link_icon = link_collection.metafields.sgl.icon -%}
        {%- endif -%}
        {%- if link_collection.metafields.sgl.badge != blank -%}
          {%- assign link_badge = link_collection.metafields.sgl.badge -%}
        {%- endif -%}

        <div class="sgl-drawer__nav-item">
          {%- if link.links.size > 0 -%}
            {%- comment -%} Has children → opens submenu panel {%- endcomment -%}
            <button class="sgl-drawer__nav-link" data-sgl-submenu-open="{{ link.handle }}" aria-expanded="false" aria-controls="sgl-submenu-{{ link.handle }}">
              <div class="sgl-drawer__nav-icon">
                {%- render 'sgl-icon', icon: link_icon, size: 22 -%}
              </div>
              <div class="sgl-drawer__nav-content">
                <span class="sgl-drawer__nav-title">{{ link.title }}</span>
                {%- if link_collection -%}
                  <span class="sgl-drawer__nav-subtitle">{{ link_collection.products_count }} products</span>
                {%- endif -%}
              </div>
              {%- if link_badge != blank -%}
                <span class="sgl-drawer__nav-badge sgl-drawer__nav-badge--{{ link_badge | downcase }}">{{ link_badge }}</span>
              {%- endif -%}
              <div class="sgl-drawer__nav-arrow">
                {%- render 'sgl-icon', icon: 'chevron-right', size: 16 -%}
              </div>
            </button>
          {%- else -%}
            {%- comment -%} No children → direct link {%- endcomment -%}
            <a href="{{ link.url }}" class="sgl-drawer__nav-link">
              <div class="sgl-drawer__nav-icon">
                {%- render 'sgl-icon', icon: link_icon, size: 22 -%}
              </div>
              <div class="sgl-drawer__nav-content">
                <span class="sgl-drawer__nav-title">{{ link.title }}</span>
                {%- if link_collection -%}
                  <span class="sgl-drawer__nav-subtitle">{{ link_collection.products_count }} products</span>
                {%- endif -%}
              </div>
              {%- if link_badge != blank -%}
                <span class="sgl-drawer__nav-badge sgl-drawer__nav-badge--{{ link_badge | downcase }}">{{ link_badge }}</span>
              {%- endif -%}
            </a>
          {%- endif -%}
        </div>
      {%- endfor -%}
    </nav>

    {%- comment -%} Divider {%- endcomment -%}
    <div class="sgl-drawer__divider"></div>

    {%- comment -%} Quick Links (pre-rendered from blocks) {%- endcomment -%}
    {%- if mobile_quick_links_html != blank -%}
      <div class="sgl-drawer__secondary">
        <div class="sgl-drawer__secondary-title">{{ mobile_quick_links_title | default: 'Quick Links' }}</div>
        <div class="sgl-drawer__secondary-links">
          {{ mobile_quick_links_html }}
        </div>
      </div>
    {%- endif -%}
  </div>

  {%- comment -%} Footer {%- endcomment -%}
  <div class="sgl-drawer__footer">
    {%- comment -%} Country/Currency Selector {%- endcomment -%}
    {%- if localization.available_countries.size > 1 or localization.available_languages.size > 1 -%}
      <button class="sgl-drawer__locale-btn" aria-label="Country and language">
        {%- render 'sgl-icon', icon: 'globe', size: 18 -%}
        <span class="sgl-drawer__locale-country">{{ localization.country.name }}</span>
        <span class="sgl-drawer__locale-currency">({{ localization.country.currency.iso_code }} {{ localization.country.currency.symbol }})</span>
        {%- render 'sgl-icon', icon: 'chevron-right', size: 14 -%}
      </button>
    {%- endif -%}

    <div class="sgl-drawer__social">
      <a href="{{ settings.social_instagram_link }}" class="sgl-drawer__social-link" aria-label="Instagram" target="_blank" rel="noopener">
        {%- render 'sgl-icon', icon: 'instagram', size: 20 -%}
      </a>
      <a href="{{ settings.social_tiktok_link }}" class="sgl-drawer__social-link" aria-label="TikTok" target="_blank" rel="noopener">
        {%- render 'sgl-icon', icon: 'tiktok', size: 20 -%}
      </a>
      <a href="{{ settings.social_pinterest_link }}" class="sgl-drawer__social-link" aria-label="Pinterest" target="_blank" rel="noopener">
        {%- render 'sgl-icon', icon: 'pinterest', size: 20 -%}
      </a>
      <a href="{{ settings.social_youtube_link }}" class="sgl-drawer__social-link" aria-label="YouTube" target="_blank" rel="noopener">
        {%- render 'sgl-icon', icon: 'youtube', size: 20 -%}
      </a>
    </div>
    <p class="sgl-drawer__copyright">&copy; {{ 'now' | date: '%Y' }} {{ logo_text | default: 'Seoul Glow Lab' }}. All rights reserved.</p>
  </div>
</div>

{%- comment -%} ── Render Submenu Panels ── {%- endcomment -%}
{%- for link in linklists[menu].links -%}
  {%- if link.links.size > 0 -%}
    {%- render 'sgl-submenu-panel', link: link, panel_id: link.handle, submenu_product_limit: submenu_product_limit, drawer_glassmorphic: drawer_glassmorphic -%}
  {%- endif -%}
{%- endfor -%}

{% stylesheet %}
  /* ── Drawer Overlay ── */
  .sgl-drawer-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    z-index: 1100;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s;
  }
  .sgl-drawer-overlay.active {
    opacity: 1;
    visibility: visible;
  }

  /* ── Drawer Panel ── */
  .sgl-drawer {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    max-width: 340px;
    height: 100%;
    background: linear-gradient(180deg, color-mix(in srgb, var(--sgl-drawer-bg, #F8EBDA) 100%, white 5%) 0%, var(--sgl-drawer-bg, #F8EBDA) 100%);
    z-index: 1200;
    transform: translateX(-100%);
    transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    flex-direction: column;
    overflow: hidden;
    color: #5E0A02;
  }
  .sgl-drawer.active {
    transform: translateX(0);
  }

  /* Ambient glow - uses drawer accent color */
  .sgl-drawer::before {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    width: 200px;
    height: 400px;
    background: radial-gradient(ellipse, rgba(var(--sgl-drawer-accent-rgb, 147, 51, 234), 0.15) 0%, transparent 70%);
    pointer-events: none;
  }
  .sgl-drawer::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 200px;
    height: 300px;
    background: radial-gradient(ellipse, rgba(var(--sgl-glow-secondary-rgb, 236, 72, 153), 0.1) 0%, transparent 70%);
    pointer-events: none;
  }

  /* ── Header ── */
  .sgl-drawer__header {
    padding: 20px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    position: relative;
    z-index: 1;
  }
  .sgl-drawer__logo {
    display: flex;
    align-items: center;
    height: var(--drawer-logo-height, 28px);
  }
  .sgl-drawer__logo-img {
    height: var(--drawer-logo-height, 28px);
    width: auto;
    object-fit: contain;
  }
  .sgl-drawer__logo-text {
    font-size: 14px;
    font-weight: 300;
    letter-spacing: 3px;
  }
  .sgl-drawer__close {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    cursor: pointer;
    transition: all 0.2s;
    color: #5E0A02;
  }
  .sgl-drawer__close:hover { background: rgba(255, 255, 255, 0.1); }

  .sgl-drawer__header-actions {
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .sgl-drawer__header-btn {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    cursor: pointer;
    transition: all 0.2s;
    color: #5E0A02;
  }
  .sgl-drawer__header-btn:hover { background: rgba(255, 255, 255, 0.1); }

  /* ── Content (scrollable) ── */
  .sgl-drawer__content {
    flex: 1;
    overflow-y: auto;
    overflow-x: hidden;
    position: relative;
    z-index: 1;
  }
  .sgl-drawer__content::-webkit-scrollbar { width: 4px; }
  .sgl-drawer__content::-webkit-scrollbar-track { background: transparent; }
  .sgl-drawer__content::-webkit-scrollbar-thumb { background: rgba(255, 255, 255, 0.1); border-radius: 4px; }

  /* ── Featured Card ── */
  .sgl-drawer__featured {
    margin: 16px;
    padding: 20px;
    background: linear-gradient(135deg, rgba(var(--sgl-drawer-accent-rgb, 147, 51, 234), 0.2), rgba(var(--sgl-glow-secondary-rgb, 236, 72, 153), 0.15));
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    position: relative;
    overflow: hidden;
  }
  .sgl-drawer__featured::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(90deg, var(--sgl-drawer-accent, #B0C3FA), var(--sgl-glow-secondary, #EA7940), var(--sgl-support-primary, #FFBF9F));
  }
  .sgl-drawer__featured-content {
    display: flex;
    gap: 16px;
    align-items: center;
  }
  .sgl-drawer__featured-visual {
    width: 70px;
    height: 70px;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    overflow: hidden;
  }
  .sgl-drawer__featured-video {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 12px;
  }
  .sgl-drawer__featured--clickable {
    display: block;
    text-decoration: none;
    color: #5E0A02;
    cursor: pointer;
    transition: transform 0.2s, border-color 0.2s;
  }
  .sgl-drawer__featured--clickable:active {
    transform: scale(0.98);
    border-color: rgba(var(--sgl-drawer-accent-rgb, 147, 51, 234), 0.4);
  }
  .sgl-drawer__featured-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 12px;
  }
  .sgl-drawer__featured-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, rgba(var(--sgl-drawer-accent-rgb, 147, 51, 234), 0.3), rgba(var(--sgl-glow-secondary-rgb, 236, 72, 153), 0.2));
    border-radius: 12px;
    color: rgba(255, 255, 255, 0.5);
  }
  .sgl-drawer__featured-label {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 10px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: var(--sgl-glow-secondary, #EA7940);
    margin-bottom: 4px;
  }
  .sgl-drawer__featured-desc {
    font-size: 12px;
    color: rgba(255, 255, 255, 0.6);
    line-height: 1.4;
    margin-bottom: 8px;
  }
  .sgl-drawer__featured-link {
    font-size: 12px;
    font-weight: 600;
    color: var(--sgl-drawer-accent, #B0C3FA);
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    gap: 4px;
    transition: gap 0.2s;
  }
  .sgl-drawer__featured-link:hover { gap: 8px; }

  /* ── Navigation Items ── */
  .sgl-drawer__nav { padding: 8px 0; }

  .sgl-drawer__nav-link {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 16px 20px;
    color: #5E0A02;
    text-decoration: none;
    transition: all 0.2s;
    background: none;
    border: none;
    width: 100%;
    text-align: left;
    cursor: pointer;
    font-family: inherit;
    font-size: inherit;
  }
  .sgl-drawer__nav-link:active {
    background: rgba(255, 255, 255, 0.05);
  }

  .sgl-drawer__nav-icon {
    width: 44px;
    height: 44px;
    border-radius: 12px;
    background: linear-gradient(135deg, rgba(var(--sgl-drawer-accent-rgb, 147, 51, 234), 0.15), rgba(var(--sgl-glow-secondary-rgb, 236, 72, 153), 0.1));
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s;
    flex-shrink: 0;
  }
  .sgl-drawer__nav-link:active .sgl-drawer__nav-icon {
    background: linear-gradient(135deg, var(--sgl-drawer-accent, #B0C3FA), var(--sgl-glow-secondary, #EA7940));
    box-shadow: 0 4px 16px rgba(var(--sgl-drawer-accent-rgb, 147, 51, 234), 0.4);
  }

  .sgl-drawer__nav-content { flex: 1; }
  .sgl-drawer__nav-title { font-size: 15px; font-weight: 500; display: block; margin-bottom: 2px; }
  .sgl-drawer__nav-subtitle { font-size: 12px; color: rgba(255, 255, 255, 0.4); display: block; }

  .sgl-drawer__nav-badge {
    font-size: 9px;
    font-weight: 600;
    padding: 4px 10px;
    border-radius: 20px;
    text-transform: uppercase;
    color: #5E0A02;
  }
  .sgl-drawer__nav-badge--hot { background: var(--sgl-gold, #F2FFA0); color: #000; }
  .sgl-drawer__nav-badge--new { background: var(--sgl-green, #1B4900); }
  .sgl-drawer__nav-badge--sale { background: var(--sgl-red, #5E0A02); }

  .sgl-drawer__nav-arrow {
    width: 28px;
    height: 28px;
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.05);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s;
    flex-shrink: 0;
    color: rgba(255, 255, 255, 0.5);
  }

  /* ── Divider ── */
  .sgl-drawer__divider {
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
    margin: 8px 20px;
  }

  /* ── Quick Links ── */
  .sgl-drawer__secondary { padding: 8px 20px 16px; }
  .sgl-drawer__secondary-title {
    font-size: 10px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1.5px;
    color: rgba(255, 255, 255, 0.3);
    margin-bottom: 12px;
    padding-left: 4px;
  }
  .sgl-drawer__secondary-links {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }
  .sgl-drawer__secondary-link {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 10px 14px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 100px;
    color: rgba(255, 255, 255, 0.6);
    text-decoration: none;
    font-size: 12px;
    transition: all 0.2s;
  }
  .sgl-drawer__secondary-link:active {
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(var(--sgl-drawer-accent-rgb, 147, 51, 234), 0.3);
    color: #5E0A02;
  }

  /* ── Footer ── */
  .sgl-drawer__footer {
    padding: 16px 20px;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    position: relative;
    z-index: 1;
  }

  /* Locale/Currency Button */
  .sgl-drawer__locale-btn {
    display: flex;
    align-items: center;
    gap: 8px;
    width: 100%;
    padding: 12px 16px;
    margin-bottom: 16px;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    color: #5E0A02;
    font-size: 13px;
    cursor: pointer;
    transition: all 0.2s;
  }
  .sgl-drawer__locale-btn:active {
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(var(--sgl-drawer-accent-rgb, 147, 51, 234), 0.3);
  }
  .sgl-drawer__locale-country {
    flex: 1;
    text-align: left;
    font-weight: 500;
  }
  .sgl-drawer__locale-currency {
    color: rgba(255, 255, 255, 0.5);
    font-size: 12px;
  }

  .sgl-drawer__social {
    display: flex;
    justify-content: center;
    gap: 12px;
    margin-bottom: 16px;
  }
  .sgl-drawer__social-link {
    width: 44px;
    height: 44px;
    border-radius: 12px;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s;
    color: #5E0A02;
  }
  .sgl-drawer__social-link:active {
    background: var(--sgl-drawer-accent, #B0C3FA);
    border-color: var(--sgl-drawer-accent, #B0C3FA);
  }
  .sgl-drawer__copyright {
    font-size: 11px;
    color: rgba(255, 255, 255, 0.3);
    text-align: center;
    margin: 0;
  }

  /* ═══════════════════════════════════════════════════════
     GLASSMORPHIC DRAWER — Apple-style frosted glass
     All colors driven by --sgl-glass-* CSS custom properties
     set in sgl-header.liquid from customizer settings.
     ═══════════════════════════════════════════════════════ */
  .sgl-drawer--glass {
    background: rgba(var(--sgl-glass-tint-rgb), var(--sgl-glass-opacity));
    backdrop-filter: blur(var(--sgl-glass-blur)) saturate(180%);
    -webkit-backdrop-filter: blur(var(--sgl-glass-blur)) saturate(180%);
    color: var(--sgl-glass-text);
  }
  .sgl-drawer--glass::before,
  .sgl-drawer--glass::after {
    display: none;
  }
  .sgl-drawer--glass .sgl-drawer__header {
    border-bottom-color: rgba(var(--sgl-glass-border-rgb), 0.25);
  }
  .sgl-drawer--glass .sgl-drawer__logo,
  .sgl-drawer--glass .sgl-drawer__logo-text {
    color: var(--sgl-glass-text);
  }
  .sgl-drawer--glass .sgl-drawer__close,
  .sgl-drawer--glass .sgl-drawer__header-btn {
    background: rgba(var(--sgl-glass-icon-bg-rgb), 0.35);
    border-color: rgba(var(--sgl-glass-border-rgb), 0.3);
    color: var(--sgl-glass-text);
  }
  .sgl-drawer--glass .sgl-drawer__close:hover,
  .sgl-drawer--glass .sgl-drawer__header-btn:hover {
    background: rgba(var(--sgl-glass-icon-bg-rgb), 0.55);
  }
  .sgl-drawer--glass .sgl-drawer__featured {
    background: rgba(var(--sgl-glass-card-bg-rgb), 0.4);
    border-color: rgba(var(--sgl-glass-border-rgb), 0.25);
  }
  .sgl-drawer--glass .sgl-drawer__featured::before {
    background: linear-gradient(90deg, rgba(var(--sgl-glass-text-rgb), 0.12), rgba(var(--sgl-glass-text-rgb), 0.06), rgba(var(--sgl-glass-text-rgb), 0.03));
  }
  .sgl-drawer--glass .sgl-drawer__featured-label {
    color: var(--sgl-glass-text-muted);
  }
  .sgl-drawer--glass .sgl-drawer__featured-desc {
    color: rgba(var(--sgl-glass-text-muted-rgb), 0.75);
  }
  .sgl-drawer--glass .sgl-drawer__featured-link {
    color: var(--sgl-glass-text);
  }
  .sgl-drawer--glass .sgl-drawer__featured-placeholder {
    background: rgba(var(--sgl-glass-icon-bg-rgb), 0.3);
    color: rgba(var(--sgl-glass-text-muted-rgb), 0.5);
  }
  .sgl-drawer--glass .sgl-drawer__nav-link {
    color: var(--sgl-glass-text);
  }
  .sgl-drawer--glass .sgl-drawer__nav-link:active {
    background: rgba(var(--sgl-glass-icon-bg-rgb), 0.3);
  }
  .sgl-drawer--glass .sgl-drawer__nav-icon {
    background: rgba(var(--sgl-glass-icon-bg-rgb), 0.4);
  }
  .sgl-drawer--glass .sgl-drawer__nav-link:active .sgl-drawer__nav-icon {
    background: var(--sgl-glass-icon-active);
    box-shadow: none;
    color: var(--sgl-glass-card-bg);
  }
  .sgl-drawer--glass .sgl-drawer__nav-subtitle {
    color: var(--sgl-glass-text-muted);
  }
  .sgl-drawer--glass .sgl-drawer__nav-arrow {
    background: rgba(var(--sgl-glass-icon-bg-rgb), 0.3);
    color: var(--sgl-glass-text-muted);
  }
  .sgl-drawer--glass .sgl-drawer__divider {
    background: linear-gradient(90deg, transparent, rgba(var(--sgl-glass-border-rgb), 0.35), transparent);
  }
  .sgl-drawer--glass .sgl-drawer__secondary-title {
    color: var(--sgl-glass-text-muted);
  }
  .sgl-drawer--glass .sgl-drawer__secondary-link {
    background: rgba(var(--sgl-glass-card-bg-rgb), 0.25);
    border-color: rgba(var(--sgl-glass-border-rgb), 0.3);
    color: var(--sgl-glass-text);
  }
  .sgl-drawer--glass .sgl-drawer__secondary-link:active {
    background: rgba(var(--sgl-glass-icon-bg-rgb), 0.45);
    border-color: rgba(var(--sgl-glass-border-rgb), 0.5);
    color: var(--sgl-glass-text);
  }
  .sgl-drawer--glass .sgl-drawer__footer {
    border-top-color: rgba(var(--sgl-glass-border-rgb), 0.25);
  }
  .sgl-drawer--glass .sgl-drawer__locale-btn {
    background: rgba(var(--sgl-glass-card-bg-rgb), 0.25);
    border-color: rgba(var(--sgl-glass-border-rgb), 0.3);
    color: var(--sgl-glass-text);
  }
  .sgl-drawer--glass .sgl-drawer__locale-btn:active {
    background: rgba(var(--sgl-glass-icon-bg-rgb), 0.45);
  }
  .sgl-drawer--glass .sgl-drawer__locale-currency {
    color: var(--sgl-glass-text-muted);
  }
  .sgl-drawer--glass .sgl-drawer__social-link {
    background: rgba(var(--sgl-glass-card-bg-rgb), 0.25);
    border-color: rgba(var(--sgl-glass-border-rgb), 0.3);
    color: var(--sgl-glass-text);
  }
  .sgl-drawer--glass .sgl-drawer__social-link:active {
    background: var(--sgl-glass-icon-active);
    border-color: var(--sgl-glass-icon-active);
    color: var(--sgl-glass-card-bg);
  }
  .sgl-drawer--glass .sgl-drawer__copyright {
    color: var(--sgl-glass-text-muted);
  }
  .sgl-drawer--glass .sgl-drawer__content::-webkit-scrollbar-thumb {
    background: rgba(var(--sgl-glass-border-rgb), 0.3);
  }

  /* ═══════════════════════════════════════════════════════
     RESPONSIVE: VERY SMALL MOBILE (375px and below)
     Optimized for iPhone SE and similar small devices
     ═══════════════════════════════════════════════════════ */
  @media (max-width: 375px) {
    .sgl-drawer {
      max-width: 100%;
    }

    .sgl-drawer__header {
      padding: 16px;
    }

    .sgl-drawer__logo { height: var(--drawer-logo-height, 24px); }
    .sgl-drawer__logo-img { height: var(--drawer-logo-height, 24px); }
    .sgl-drawer__logo-text {
      font-size: 12px;
      letter-spacing: 2px;
    }

    .sgl-drawer__close,
    .sgl-drawer__header-btn {
      width: 36px;
      height: 36px;
      border-radius: 10px;
    }

    .sgl-drawer__header-actions {
      gap: 6px;
    }

    .sgl-drawer__featured {
      margin: 12px;
      padding: 14px;
    }

    .sgl-drawer__featured-visual {
      width: 56px;
      height: 56px;
      border-radius: 10px;
    }

    .sgl-drawer__featured-content {
      gap: 12px;
    }

    .sgl-drawer__featured-label {
      font-size: 9px;
    }

    .sgl-drawer__featured-desc {
      font-size: 11px;
    }

    .sgl-drawer__featured-link {
      font-size: 11px;
    }

    .sgl-drawer__nav-link {
      padding: 12px 16px;
      gap: 12px;
    }

    .sgl-drawer__nav-icon {
      width: 38px;
      height: 38px;
      border-radius: 10px;
    }

    .sgl-drawer__nav-icon svg {
      width: 18px;
      height: 18px;
    }

    .sgl-drawer__nav-title {
      font-size: 14px;
    }

    .sgl-drawer__nav-subtitle {
      font-size: 11px;
    }

    .sgl-drawer__nav-badge {
      font-size: 8px;
      padding: 3px 8px;
    }

    .sgl-drawer__nav-arrow {
      width: 24px;
      height: 24px;
      border-radius: 6px;
    }

    .sgl-drawer__divider {
      margin: 6px 16px;
    }

    .sgl-drawer__secondary {
      padding: 6px 16px 12px;
    }

    .sgl-drawer__secondary-title {
      font-size: 9px;
      margin-bottom: 10px;
    }

    .sgl-drawer__secondary-links {
      gap: 6px;
    }

    .sgl-drawer__secondary-link {
      padding: 8px 12px;
      font-size: 11px;
    }

    .sgl-drawer__footer {
      padding: 12px 16px;
    }

    .sgl-drawer__locale-btn {
      padding: 10px 14px;
      font-size: 12px;
      margin-bottom: 12px;
    }

    .sgl-drawer__social {
      gap: 10px;
      margin-bottom: 12px;
    }

    .sgl-drawer__social-link {
      width: 38px;
      height: 38px;
      border-radius: 10px;
    }

    .sgl-drawer__social-link svg {
      width: 18px;
      height: 18px;
    }

    .sgl-drawer__copyright {
      font-size: 10px;
    }
  }

  /* ═══════════════════════════════════════════════════════
     RESPONSIVE: EXTRA SMALL MOBILE (320px and below)
     For very narrow screens
     ═══════════════════════════════════════════════════════ */
  @media (max-width: 320px) {
    .sgl-drawer__header {
      padding: 12px;
    }

    .sgl-drawer__logo { height: var(--drawer-logo-height, 20px); }
    .sgl-drawer__logo-img { height: var(--drawer-logo-height, 20px); }
    .sgl-drawer__logo-text {
      font-size: 11px;
      letter-spacing: 1.5px;
    }

    .sgl-drawer__close,
    .sgl-drawer__header-btn {
      width: 32px;
      height: 32px;
    }

    .sgl-drawer__featured {
      margin: 10px;
      padding: 12px;
    }

    .sgl-drawer__featured-visual {
      width: 48px;
      height: 48px;
    }

    .sgl-drawer__nav-link {
      padding: 10px 12px;
      gap: 10px;
    }

    .sgl-drawer__nav-icon {
      width: 34px;
      height: 34px;
    }

    .sgl-drawer__nav-title {
      font-size: 13px;
    }

    .sgl-drawer__nav-badge {
      display: none;
    }

    .sgl-drawer__secondary {
      padding: 4px 12px 10px;
    }

    .sgl-drawer__secondary-link {
      padding: 6px 10px;
      font-size: 10px;
    }

    .sgl-drawer__footer {
      padding: 10px 12px;
    }

    .sgl-drawer__social-link {
      width: 34px;
      height: 34px;
    }
  }
{% endstylesheet %}
